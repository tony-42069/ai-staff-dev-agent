# {{ project_name }}

A Python package generated by AI Staff Development Agent.

## Installation

```bash
pip install {{ project_name }}
```

## Quick Start

```python
from {{ project_name }}.core import Example

example = Example()
result = example.run()
print(result)  # Hello from {{ project_name }}!
```

## Features

- Modern Python package structure
- Full test coverage with pytest
- Type hints and static type checking with mypy
- Code formatting with black and isort
- Documentation with MkDocs and mkdocstrings
- Continuous Integration with GitHub Actions
- Docker support for containerized development

## Development

1. Clone the repository:
```bash
git clone https://github.com/yourusername/{{ project_name }}.git
cd {{ project_name }}
```

2. Create a virtual environment:
```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

3. Install development dependencies:
```bash
pip install -r requirements.txt
```

4. Run tests:
```bash
pytest
```

5. Format code:
```bash
black .
isort .
```

6. Type check:
```bash
mypy src/{{ project_name }}
```

7. Build documentation:
```bash
mkdocs serve
```

## Docker

Build and run with Docker:

```bash
docker build -t {{ project_name }} .
docker run {{ project_name }}
```

Or use Docker Compose:

```bash
docker-compose up
```

## Documentation

Full documentation is available at: https://yourusername.github.io/{{ project_name }}

## Contributing

1. Fork the repository
2. Create a feature branch
3. Make your changes
4. Run tests
5. Submit a pull request

## License

{{ license }} License - see [LICENSE](LICENSE) for details.
